<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($title); ?></title>
    <link rel="stylesheet" href="assets/css/style.css">
    <?php if (in_array($_GET["page"] ?? "", ["create", "edit", "dashboard"])): ?>
        <link href="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.snow.css" rel="stylesheet">
    <?php endif; ?>
    <script src="assets/js/script.js" defer></script>
    <?php if (in_array($_GET["page"] ?? "", ["create", "edit", "dashboard"])): ?>
        <script src="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.js"></script>
    <?php endif; ?>
    <?php $jsPage = "assets/js/" . htmlspecialchars($_GET["page"] ?? "home") . ".js"; ?>
    <?php if (file_exists($jsPage)): ?>
        <script src="<?= $jsPage ?>" defer></script>
    <?php endif; ?>
</head>
<body>

    <header>
        <div class="Container">
            <a href="?page=home"><h1><?= htmlspecialchars($layoutName); ?></h1></a>

            <?php if (!empty($_SESSION["user"])): ?>
                <div class="Btn_Container">
                    <a href="?page=dashboard" class="btn">Dashboard</a>
                    <a href="?page=logout" class="btn">Déconnexion</a>
                </div>
            <?php else: ?>
                <div class="Btn_Container">
                    <a href="?page=login" class="btn">Connexion</a>
                </div>
            <?php endif; ?>


        </div>
    </header>

    <?php include $content; ?>
    
    <footer>

        <p>&copy; <?= htmlspecialchars($layoutName); ?> | 2026</p>
        
    </footer>

</body>
</html>